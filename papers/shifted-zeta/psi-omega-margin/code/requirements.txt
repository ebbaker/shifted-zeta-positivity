# Environment for the psi_omega_margin verification scripts (c1–c8).
# These are the versions used for the 27 Aug 2026 recomputation; the scripts use
# only long-stable APIs and run under any recent mpmath/numpy.
mpmath==1.4.1
numpy==2.4.4
# sympy is not required by c1–c8; the repository-wide environment/requirements.txt
# pins it for other code in this project.
