# Pinned environment for the recomputation and laboratory scripts.
# Versions are those used for the referee-round recomputations (31 Aug – 5 Sept 2026).
mpmath==1.4.1
numpy==2.4.4
scipy==1.17.1
sympy>=1.12
# The primary first-slab certificate additionally requires python-flint (Arb);
# the exact version is recorded at code release (checklist item D4).
